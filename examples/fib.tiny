// Example tiny program
fn fib(n: int) -> int {
  if (n <= 1) {
    return n;
  } else {
    return fib(n - 1) + fib(n - 2);
  }
}

fn main() -> int {
  let x: int = 10;
  let y: int = fib(x);
  return y;
}
